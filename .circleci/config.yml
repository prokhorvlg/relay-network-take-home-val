# CircleCI configuration file
version: 2.1

jobs:
  build:
    working_directory: /app
    docker:
     - image: cimg/base:stable
    steps:
      - run: sudo circleci build

workflows:
  deploy_app:
    jobs:
      - build